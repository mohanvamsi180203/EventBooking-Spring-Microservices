package com.booking.service.service;

import java.util.List;

import com.booking.service.dto.BookingResponseDto;
import com.booking.service.entity.Booking;
public interface BookingService {

    BookingResponseDto blockTickets(Booking booking);

    BookingResponseDto confirmBooking(Integer bookingId);

    Booking cancelBooking(Integer bookingId);

    String expireBooking(Integer bookingId);

    List<BookingResponseDto> viewAll();

    BookingResponseDto getById(Integer id);

    List<BookingResponseDto> getByUser(Integer userId);

    List<BookingResponseDto> getByEvent(Integer eventId);

    List<BookingResponseDto> getByStatus(String status);
}